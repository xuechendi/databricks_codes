# {{ model_name }} training
This is an auto-generated notebook. To reproduce these results, attach this notebook to the **{{ cluster_name }}** cluster and rerun it.
- Compare trials in the [MLflow experiment]({{ experiment_url }})
- Navigate to the parent notebook [here]({{ driver_notebook_url }}) (If you launched the AutoML experiment using the Experiments UI, this link isn't very useful.)
- Clone this notebook into your project folder by selecting **File > Clone** in the notebook toolbar.

Runtime Version: _{{ runtime_version }}_